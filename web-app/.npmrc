# Use legacy peer deps to avoid issues with React 18
legacy-peer-deps=true

# Force use of npm registry
registry=https://registry.npmjs.org/

# Avoid optional dependencies
optional=false

# Do not save to package.json
save=false 